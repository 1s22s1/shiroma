#include <bits/stdc++.h>

using namespace std;

#define endl '\n'
#define ll long long
#define rep(i, n) for (ll i = 0; i < (ll)(n); i++)

void init()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
}

void solve(void)
{
    ll na, nb, nc;
    cin >> na >> nb >> nc;

    ll answer = -1;

    ll a_na = na;
    ll b_na = nb;
    ll c_na = nc;

    return;
}

int main()
{
    init();

    ll T;
    cin >> T;

    rep(i, T)
    {
        solve();
    }

    return 0;
}
